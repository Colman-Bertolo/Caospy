[tox]
envlist = docstyle, py39, style, coverage, check-manifest


[gh-actions]
python =
    3.9: py39, style, coverage, check-manifest 

[testenv]
deps = pytest
commands = pytest tests/  {posargs}

[testenv:style]
deps = 
       flake8
       flake8-import-order
       flake8-black
commands =
           flake8 setup.py tests/ caospy/ {posargs}

[testenv:coverage]
deps =
    coverage
    pytest-cov
commands =
    - coverage erase
    - pytest -q tests/ --cov=caospy --cov-append --cov-report=
    coverage report --fail-under=95 -m

[testenv:check-manifest]
skip_install = true
deps = check-manifest
commands = check-manifest

[testenv:docstyle]
deps = pydocstyle
commands =
    pydocstyle caospy --convention=numpy
